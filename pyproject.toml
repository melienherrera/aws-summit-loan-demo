[project]
name = "loan-demo"
version = "0.1.0"
description = "AWS Summit booth demo: Temporal + Strands Agents + AWS Bedrock loan underwriting"
requires-python = ">=3.10"
dependencies = [
    "strands-agents>=1.39.0",
    "strands-agents-tools>=0.5.2",
    "temporalio[strands-agents,pydantic]>=1.28.0",
    "boto3[crt]>=1.34.92,<2",
    "python-dotenv",
    "nanoid",
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "botocore[crt]>=1.43.23",
]

[tool.uv]
package = false
