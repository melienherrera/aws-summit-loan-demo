# ── Local Development ─────────────────────────────────────────────
# Temporal dev server (default for local runs)
TEMPORAL_ADDRESS=localhost:7233
TEMPORAL_NAMESPACE=default

# ── Temporal Cloud ────────────────────────────────────────────────
# Uncomment and fill in when connecting to Temporal Cloud
# TEMPORAL_ADDRESS=<namespace>.<account>.tmprl.cloud:7233
# TEMPORAL_NAMESPACE=<namespace>
# TEMPORAL_API_KEY=<your-api-key>

# ── AWS Credentials (local development) ──────────────────────────
AWS_REGION=us-west-2

# Option 1: SSO / bearer token (recommended for local dev)
# AWS_BEARER_TOKEN_BEDROCK=your-bedrock-bearer-token

# Option 2: IAM access keys
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key

# Option 3: Named SSO profile
# AWS_PROFILE=your-sso-profile

# ── Failure Mode Demos ────────────────────────────────────────────
# Demo 1: Flaky credit bureau API (credit_check activity)
# Simulates N consecutive 429s — Temporal retries automatically, no delay.
# DEMO_API_RETRY=true
# DEMO_API_RETRY_FAILURES=4   # failures before success (default: 4)

# Demo 2: Worker crash (calculate_debt_to_income activity)
# Pauses the activity for N seconds — kill the worker during that window.
# Temporal resumes automatically when the worker restarts.
# DEMO_CRASH_DELAY=15

# ── Lambda Deployment (Serverless Workers) ────────────────────────
# Only needed when building/deploying the Lambda worker (steps 3–11)
# AWS_ACCOUNT_ID=123456789012
# LAMBDA_FUNCTION_NAME=loan-underwriting-worker
# LAMBDA_S3_BUCKET=loan-underwriting-lambda-deploy
# LAMBDA_REGION=us-west-2
